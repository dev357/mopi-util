{
  "name": "mopi-util",
  "version": "0.0.1",
  "description": "Shut down RPi when main power is disconnected from MoPi",
  "main": "main.js",
  "scripts": {
    "start": "sudo node main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dev357/mopi-util.git"
  },
  "author": "Risto",
  "license": "ISC",
  "dependencies": {
    "i2c": "https://github.com/polaris/node-i2c"
  },
  "devDependencies": {
    "eslint": "^1.10.3",
    "eslint-config-defaults": "^8.0.2",
    "eslint-plugin-filenames": "^0.2.0"
  },
  "eslintConfig": {
    "env": {
      "node": true,
      "es6": true
    },
    "extends": [
      "defaults"
    ],
    "rules": {
      "no-console": 0
    }
  }
}
